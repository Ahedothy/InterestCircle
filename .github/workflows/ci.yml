name: CI Workflow

on: 
  workflow_dispatch:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '20.15.0'

      - name: Set up MySQL
        uses: mysql/setup-mysql@v2
        with:
          mysql-version: '8.0.37'
          mysql-port: 3306
        
      - name: Login to MySQL
        run: |
           echo "${{ secrets.DB_PASSWORD }}" | mysql -u root -p
        env:
          DB_PASSWORD: ${{ secrets.DB_PASSWORD }}
      
      - name: Start MySQL service
        run: |
          net start mysql80 

      - name: Install backend dependencies
        working-directory: backend 
        run: npm install 

      - name: Run backend
        working-directory: backend
        run: npm run dev 

      - name: Install frontend dependencies
        working-directory: frontend
        run: npm install

      - name: Run frontend
        working-directory: frontend
        run: npm run dev
